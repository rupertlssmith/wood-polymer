### Wood UI elements in Elm plus Polymer

## Usage

Install dependencies:

    $ npm install
    $ npm install -g grunt-cli

Build and serve up the application with:

    $ grunt dev

Use the application by openeing http://localhost:9072/
